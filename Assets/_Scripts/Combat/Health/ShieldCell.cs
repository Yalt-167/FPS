using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShieldCell
{
    private float currentHealth;
    private float maxHealth;

    public ShieldCell(float maxHealth_)
    {
        maxHealth = maxHealth_;
    }
}
